# A benchmark of ensemble methods for spatial interpolation on large datasets


To produce the benchmark : 
Always cd benchmark_spatial_interpolation/
Authorize to write with chmod +x init.sh
Then run : ./init.sh

